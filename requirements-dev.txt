# Development / Build dependencies
# Install with: pip install -r requirements-dev.txt

-r requirements.txt     # Include all runtime dependencies

pyinstaller~=6.18.0     # Only needed to build the standalone executable